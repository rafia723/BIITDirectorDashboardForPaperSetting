import 'package:flutter/material.dart';

class TopicsProgress extends StatefulWidget {
  const TopicsProgress({super.key});

  @override
  State<TopicsProgress> createState() => _TopicsProgressState();
}

class _TopicsProgressState extends State<TopicsProgress> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}